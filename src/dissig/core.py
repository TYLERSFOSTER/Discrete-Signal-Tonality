"""
core.py

Contains the central logic and primary interface for the `dissig` package.
"""
from __future__ import annotations

#from dissig.utils import
from .signal import Signal

class DissigEngine:
    """Core class representing the main analysis/synthesis engine."""

    # def __init__(self, config):

    # def process(self, data):
    #     """Run main processing pipeline on data."""

# def run_pipeline(data, config):
#     """Convenience function to run the full pipeline without using the class."""

